<% previous_has_content = true %><% content.each do |dt, dd| %><%
  if previous_has_content %>.PP
<%
  end
  if !previous_has_content %>, <% end
%><%= manify dt.text, !dd.nil? %><%
  if !dd.nil?
    previous_has_content = true %>.RS 4
<%= manify dd.text, false %><%
  if dd.blocks? %>
<%= dd.content.rstrip + "\n" %><%
  else
%><%= "\n" %><%
  end %>.RE
<%
  else
    previous_has_content = false
  end
end %>
