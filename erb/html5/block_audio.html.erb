<%#encoding:UTF-8%><div<%= @id && %( id="#{@id}") %> class="audioblock<%= attr?('style') ? %( #{attr 'style'}) : nil %><%= attr?('role') ? %( #{attr 'role'}) : nil %>"><%= title? ? %(
<div class="title">#{@caption}#{title}</div>) : nil %>
<div class="content">
<audio src="<%= media_uri(attr 'target') %>"<% if
attr? 'autoplay-option' %> autoplay<% end %><%
unless attr? 'nocontrols-option' %> controls<% end %><%
if attr? 'loop-option' %> loop<% end %>>
Your browser does not support the audio tag.
</audio>
</div>
</div>
