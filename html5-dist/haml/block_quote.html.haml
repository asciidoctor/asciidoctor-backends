%div{:id=>id, :class=>['quoteblock', (attr :role)]}
  - if title
    .title=title
  .content=content
  .attribution
    - if attr? :citetitle
      %em=attr :citetitle
    - if attr? :attribution
      - if attr? :citetitle
        %br
      ="&#8212; #{attr :attribution}"
